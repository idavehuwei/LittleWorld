# 《哼哼的小小世界》第一人称玩家方案

## 1. 根节点选择

玩家根节点使用 `CharacterBody3D`。

它适合由玩家输入直接控制、同时需要与静态世界碰撞的角色，Godot 已提供：

- `velocity`：角色当前速度。
- `move_and_slide()`：移动并沿碰撞表面滑动。
- `is_on_floor()`：判断是否站在地面。
- `floor_snap_length`：贴地，减少下坡和方块边缘的轻微悬空。
- `get_slide_collision()`：需要时读取墙体或地面碰撞结果。

不使用 `RigidBody3D`：第一人称角色需要确定、可控的移动，不应完全由物理冲量驱动。

不使用普通 `Node3D`：它没有角色碰撞求解，手写防穿透和斜面处理成本高。

## 2. 节点树

当前 Demo 由代码创建以下结构：

```text
Player (CharacterBody3D)            # 脚底原点，负责移动和水平旋转
├── BodyCollision (CollisionShape3D)
│   └── CapsuleShape3D              # 高 1.80m，半径 0.35m
└── Head (Node3D)                   # 位于脚底上方 1.62m，只负责俯仰
    └── Camera3D                    # 第一人称眼睛
```

UI 准星不挂在 Camera3D 下，而放在独立的 HUD `CanvasLayer` 中，避免受到三维透视和相机旋转影响。

后续可以在 `Head` 下增加：

```text
Head
├── Camera3D
├── BlockRayCast3D                  # 可选；当前项目用代码物理射线
└── HandAnchor (Marker3D)           # 手、工具或手持方块挂点
```

## 3. 尺寸与原点约定

- 玩家总高：`1.80m`
- 胶囊半径：`0.35m`
- 胶囊中心：局部 `y=0.90m`
- 眼睛高度：局部 `y=1.62m`
- `CharacterBody3D` 原点：脚底

脚底原点有三个好处：

1. 出生点直接表示脚落在哪里。
2. 判断玩家是否与待放置方块重叠更直观。
3. 存档和传送无需额外减去半个身高。

胶囊比方盒更适合作为角色碰撞：走过方块边缘时不容易被角卡住，滑墙也更平稳。

## 4. 视角旋转

鼠标水平位移控制整个 `CharacterBody3D` 绕 Y 轴旋转；这也决定 WASD 的移动朝向。

鼠标垂直位移只控制 `Head` 绕 X 轴旋转。俯仰限制在 `-89°..89°`，避免相机翻转。

```gdscript
func apply_look_delta(relative: Vector2) -> void:
    rotate_y(-relative.x * MOUSE_SENSITIVITY)
    head.rotation.x = clampf(
        head.rotation.x - relative.y * MOUSE_SENSITIVITY,
        -MAX_LOOK_ANGLE,
        MAX_LOOK_ANGLE
    )
```

将 Head 和身体旋转分开后，向上看不会倾斜胶囊，碰撞体始终保持竖直。

鼠标进入游戏时设为 `Input.MOUSE_MODE_CAPTURED`；按 `Esc` 释放，再点击窗口重新捕获。

## 5. WASD 移动

输入使用 Godot InputMap 动作：

- `move_forward` → W
- `move_back` → S
- `move_left` → A
- `move_right` → D
- `jump` → Space

读取二维输入后，转换成玩家本地朝向：

```gdscript
var input_vector := Input.get_vector(
    &"move_left", &"move_right",
    &"move_forward", &"move_back"
)
var direction := (
    transform.basis * Vector3(input_vector.x, 0.0, input_vector.y)
).normalized()
```

Y 分量固定为 0，所以抬头和低头不会让 WASD 飞向天空或钻进地面。

移动使用目标速度和加速度，而不是瞬间赋值：

- 最高步行速度：`6m/s`
- 地面加速度：`28m/s²`
- 空中加速度：`8m/s²`

这样起步和停止更稳定，空中仍保留有限方向控制，但不会像地面一样灵敏。

## 6. 重力、掉落与跳跃

所有运动都放在 `_physics_process(delta)` 中，因为碰撞检测必须按物理帧执行。

顺序为：

```text
应用重力
→ 检查跳跃
→ 计算水平目标速度
→ move_and_slide()
→ 更新准星射线目标
```

重力逻辑：

```gdscript
if is_on_floor():
    if velocity.y < 0.0:
        velocity.y = 0.0
else:
    velocity.y -= gravity * delta
```

跳跃只在 `is_on_floor()` 为真时触发，避免无限空中跳跃：

```gdscript
if Input.is_action_just_pressed(&"jump") and is_on_floor():
    velocity.y = JUMP_VELOCITY
```

当前参数：

- 重力：`18m/s²`
- 起跳速度：`7m/s`

理论跳跃高度约为 `v² / (2g) = 1.36m`，足以跳上 1 米高的方块，同时不会显得过分漂浮。

## 7. 玩家与方块碰撞

碰撞层约定：

```text
Layer 1：方块世界
Layer 2：玩家
```

玩家：

```gdscript
collision_layer = 2
collision_mask = 1
```

世界：

```gdscript
collision_layer = 1
collision_mask = 0
```

玩家只检测世界层。方块碰撞由当前 GridMap 的 MeshLibrary 中 `BoxShape3D` 提供，所以：

- 玩家会落到草方块顶面 y=1。
- `move_and_slide()` 会阻止胶囊穿过方块。
- 碰到墙体时会沿墙面滑动。
- 破坏方块后，GridMap 对应单元碰撞消失。
- 放置方块后，GridMap 对应单元碰撞出现。

目前玩家放置方块前还会检查目标单元是否与自身胶囊所在空间重叠，防止把玩家封进新方块中。

## 8. 脚本职责

### `scripts/player.gd`

负责玩家本体行为：

- 构建胶囊、Head 和 Camera3D。
- 读取 WASD、空格和鼠标输入。
- 计算水平速度、重力和跳跃。
- 调用 `move_and_slide()` 完成碰撞移动。
- 控制身体 yaw 与 Head pitch。
- 从摄像机发射选块射线。
- 调用世界的放置/破坏接口。
- 管理当前快捷栏方块类型。

### `scripts/main.gd`

负责游戏组装：

- 创建世界。
- 创建玩家并注入 `world` 引用。
- 设置出生位置。
- 注册 InputMap。
- 连接玩家与 HUD 信号。

### `scripts/voxel_world.gd`

负责方块世界：

- 方块数据和 MeshLibrary。
- 世界碰撞层。
- 坐标换算。
- 放置、破坏和查询方块。
- 方块选中高亮。

### `scripts/hud.gd`

只负责界面：

- 中央准星。
- 操作提示。
- 当前方块类型和快捷栏。

HUD 不直接读取键盘，也不修改玩家速度，避免控制逻辑分散。

## 9. 推荐后续扩展

保持当前节点树不变，可以逐步加入：

1. **疾跑**：按 Shift 提高目标速度，不直接修改位置。
2. **蹲下**：Tween Head 高度，同时缩短 CapsuleShape3D；起身前向上检测空间。
3. **台阶辅助**：方块世界严格为 1 米台阶，不能靠普通 `floor_max_angle` 自动跨越；需要跳跃或专门的 step-up 检测。
4. **落地反馈**：记录离地时的向下速度，落地时播放音效和轻微相机位移。
5. **脚步声**：读取脚下方块 ID，选择草地、石头或木板声音。
6. **游泳/飞行**：作为独立移动状态，不要把所有分支塞进基础地面移动函数。
7. **多人同步**：本地玩家拥有 Camera3D；远端玩家只有可见模型和同步碰撞代理。

核心原则是：`CharacterBody3D` 管移动，`Head` 管俯仰，`Camera3D` 管观察，世界管方块数据与碰撞，HUD 管显示。
