# 《哼哼的小小世界》环境光照与昼夜循环方案

## 1. 系统目标

环境系统需要同时控制三件事：

1. 天空外观随时间变化。
2. 主方向光模拟太阳绕世界旋转。
3. 整体环境亮度从白天平滑过渡到夜晚，同时让夜晚保留最低可见度。

这三个参数必须由同一个“游戏时间”驱动，避免出现太阳已经落山但天空仍是正午蓝色，或天空变黑但地面仍被强环境光照亮的情况。

## 2. 节点和资源结构

当前项目使用纯代码构建：

```text
HengHengLittleWorld (Node3D)
├── DayNightCycle (Node3D)
│   ├── WorldEnvironment
│   │   └── Environment
│   │       └── Sky
│   │           └── ProceduralSkyMaterial
│   └── Sun (DirectionalLight3D)
├── VoxelWorld (GridMap)
├── Player (CharacterBody3D)
└── HUD (CanvasLayer)
```

### DayNightCycle

独立脚本 `scripts/day_night_cycle.gd`，集中负责：

- 游戏时间推进。
- 太阳角度。
- 天空颜色。
- 环境光颜色与亮度。
- 白天、黎明、黄昏、夜晚的视觉过渡。

Main 只实例化它，不再直接管理 Environment 和 Sun。

### WorldEnvironment + Environment

`WorldEnvironment` 把一份 `Environment` 应用到整个 Viewport。

Environment 配置：

```gdscript
environment.background_mode = Environment.BG_SKY
environment.ambient_light_source = Environment.AMBIENT_SOURCE_COLOR
environment.reflected_light_source = Environment.REFLECTION_SOURCE_SKY
environment.tonemap_mode = Environment.TONE_MAPPER_AGX
```

- 背景来源使用 Sky，而不是固定背景色。
- 环境光颜色由昼夜系统直接控制，确保夜间亮度可控。
- 反射来源使用天空，让方块材质的环境反射与天空一致。
- AGX 色调映射可以保留日出日落暖色和天空高光层次。

### Sky + ProceduralSkyMaterial

`Sky` 使用 `ProceduralSkyMaterial`：

- `sky_top_color`：头顶天空颜色。
- `sky_horizon_color`：地平线颜色。
- `ground_bottom_color`：天空球下半部颜色。
- `ground_horizon_color`：下半部地平线颜色。
- `sky_energy_multiplier`：天空材质能量。

当前选择 ProceduralSky 的原因：

- 不需要外部 HDRI 素材。
- 可以运行时平滑修改颜色。
- 很适合风格化方块世界。
- 后续可在同一系统中加入云层 Shader 或星空层。

如果以后使用自定义天空盒，可将 `ProceduralSkyMaterial` 换成 `PanoramaSkyMaterial`，通过多张黎明/白天/黄昏/夜晚全景纹理交叉淡化。时间逻辑和太阳逻辑无需改变。

### DirectionalLight3D

方向光命名为 `Sun`，用于模拟无限远的太阳：

```gdscript
sun.shadow_enabled = true
sun.directional_shadow_max_distance = 90.0
sun.light_angular_distance = 0.8
```

DirectionalLight3D 的位置不影响照明，只有旋转方向重要，因此无需真的围绕世界中心改变坐标；让它绕 X 轴旋转等价于太阳在天空中绕世界运行。

## 3. 时间数据

时间使用 0～1 的归一化值：

```text
0.00 = 00:00 午夜
0.25 = 06:00 日出
0.50 = 12:00 正午
0.75 = 18:00 日落
1.00 = 次日 00:00
```

变量：

```gdscript
var time_of_day: float
```

游戏小时换算：

```gdscript
var hour := time_of_day * 24.0
```

当前默认从 08:00 开始，玩家进入世界后能够立即看清地形，同时很快观察到太阳变化。

## 4. 十分钟完整循环

完整一天长度：

```gdscript
const DAY_DURATION_SECONDS := 600.0
```

在 `_process(delta)` 中：

```gdscript
time_of_day = fposmod(
    time_of_day + delta * time_scale / DAY_DURATION_SECONDS,
    1.0
)
```

- 现实 600 秒对应游戏 24 小时。
- 现实 25 秒约等于游戏 1 小时。
- `fposmod(..., 1.0)` 让时间到达 1 后自动回到 0。
- `time_scale` 默认 1，可用于调试加速、暂停或剧情时间控制。
- `running=false` 可暂停昼夜循环。

使用 `_process()` 而不是 `_physics_process()`，因为环境颜色属于视觉更新，不需要参与角色碰撞物理。

## 5. 太阳旋转

太阳俯仰角：

```gdscript
var sun_pitch := time_of_day * 360.0 - 90.0
sun.rotation_degrees = Vector3(sun_pitch, -28.0, 0.0)
```

关键时刻：

```text
00:00 → -90°，太阳位于世界下方
06:00 →   0°，太阳位于东方地平线
12:00 →  90°，太阳处于最高位置
18:00 → 180°，太阳位于西方地平线
24:00 → 270°，回到世界下方
```

Y 轴固定为 `-28°`，让阴影斜向投射，比完全沿世界 X/Z 轴移动更有立体感。

这里说“围绕世界中心旋转”是视觉意义上的天球旋转。DirectionalLight3D 没有距离衰减，因此不需要改变光源位置。

## 6. 白昼权重计算

先从时间计算太阳高度近似值：

```gdscript
var solar_height := sin((time_of_day - 0.25) * TAU)
```

结果：

- 正午接近 1。
- 日出和日落接近 0。
- 午夜接近 -1。

再用 `smoothstep()` 生成平滑白昼权重：

```gdscript
var daylight := smoothstep(-0.16, 0.24, solar_height)
```

`daylight` 范围为 0～1，用于统一控制天空和照明。阈值不是严格天文学模型，而是为 10 分钟短周期优化：黎明和黄昏不会一闪而过，夜晚也有足够持续时间。

另外计算地平线暖色权重：

```gdscript
var dawn_distance := absf(time_of_day - 0.25)
var dusk_distance := absf(time_of_day - 0.75)
var horizon_weight := 1.0 - smoothstep(
    0.0,
    0.11,
    minf(dawn_distance, dusk_distance)
)
```

它只在 06:00 和 18:00 附近升高，用于叠加橙红色地平线与暖色太阳。

## 7. 天空颜色变化

基础颜色组：

```text
夜间头顶：深蓝黑 #071127
夜间地平线：深蓝 #17264A
白天头顶：蓝色 #4C9FE8
白天地平线：浅蓝 #B9E5FF
晨昏地平线：暖橙 #F09A69
```

计算顺序：

1. 用 `daylight` 在夜间色和白天色之间插值。
2. 用 `horizon_weight` 在日出日落附近叠加暖色。
3. 地面半球颜色使用更暗的同系颜色。
4. 环境背景能量由夜间 0.22 过渡到白天 1.0。

```gdscript
sky_top = NIGHT_SKY_TOP.lerp(DAY_SKY_TOP, daylight)
sky_horizon = NIGHT_SKY_HORIZON.lerp(DAY_SKY_HORIZON, daylight)
sky_horizon = sky_horizon.lerp(SUNSET_SKY_HORIZON, horizon_weight)
```

## 8. 环境光与夜间最低可见度

白天：

```text
环境光能量：0.72
太阳能量：1.18
背景天空能量：1.0
```

夜晚：

```text
环境光能量：0.14
冷色月光：0.055
背景天空能量：0.22
```

夜间不是简单把太阳关闭并让环境光归零，而是保留：

- 深蓝天空。
- 低强度蓝色环境光。
- 极弱冷色方向光，用于保持方块边缘和朝向可辨认。

因此夜晚明显比白天暗，但不会完全黑屏。后续加入火把后，可再把夜间最低环境光降到 0.08～0.10，让局部光源价值更明显。

太阳颜色同样变化：

- 白天：暖白 `#FFF1C9`
- 日出日落：橙色 `#FF9B63`
- 夜间弱方向光：冷蓝 `#7082AD`

阴影只在 `daylight > 0.08` 时开启，避免极低能量夜光仍产生不自然的锐利阴影，也可减少夜间渲染成本。

## 9. 关键接口

```gdscript
set_time_of_day(normalized_time: float)
set_hour(hour: float)
get_hour() -> float
get_daylight_factor() -> float
```

用途：

```gdscript
day_night.set_hour(12.0)   # 跳到正午
day_night.set_hour(0.0)    # 跳到午夜
day_night.time_scale = 20  # 调试加速
day_night.running = false  # 暂停时间
```

系统发出：

```gdscript
signal time_changed(normalized_time: float, hour: float)
```

HUD、音乐、NPC 作息和刷怪系统可以订阅该信号，但不应该直接修改太阳节点。

## 10. 每帧推进流程

```text
delta / 600 秒
      ↓
更新 time_of_day 并循环到 0～1
      ↓
计算太阳高度 daylight
      ↓
计算黎明/黄昏 horizon_weight
      ↓
旋转 DirectionalLight3D
      ↓
插值天空顶色、地平线色和地面色
      ↓
插值环境光颜色和亮度
      ↓
插值太阳颜色和能量
      ↓
发出 time_changed
```

## 11. 后续扩展

1. **星空**：夜间增加第二个天空材质层或全屏星空 Shader，透明度使用 `1-daylight`。
2. **月亮**：增加第二盏低能量 DirectionalLight3D，与太阳相差 180°。
3. **云层**：用大型球壳 Shader 或天空 Shader，根据时间改变受光颜色。
4. **雾**：夜间和黎明提高体积雾密度，但当前 GL Compatibility 模式需评估平台支持。
5. **天气**：把晴天颜色组抽成 `EnvironmentPreset` Resource，雨天降低太阳能量并提高灰蓝环境光。
6. **存档**：保存 `time_of_day`，读档后调用 `set_time_of_day()`。
7. **HUD 时钟**：订阅 `time_changed`，不要每帧自行重复计算游戏时间。

核心原则：时间是唯一状态源；太阳、天空、环境光、音乐和游戏规则都从时间派生。
