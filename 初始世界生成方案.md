# 《哼哼的小小世界》初始世界生成方案

## 1. 世界规格与坐标约定

初始世界固定为一块 `250 × 250` 的单层平地：

- X 方向：250 格，逻辑坐标 `-125..124`
- Z 方向：250 格，逻辑坐标 `-125..124`
- Y 方向：初始只有第 0 层
- 方块尺寸：`1m × 1m × 1m`
- 初始方块数量：`250 × 250 = 62,500`
- 初始方块类型：全部为草方块
- `cell.y == 0` 的方块底面位于世界 `y=0`
- `cell.y == 0` 的方块中心位于世界 `y=0.5`
- `cell.y == 0` 的方块顶面位于世界 `y=1`

Godot 的 `BoxMesh` 以中心为原点，而 GridMap 默认开启 `cell_center_y`：`map_to_local(Vector3i(x, 0, z))` 返回的中心高度已经是 `y=0.5`。因此 `VoxelWorld` 节点保持在世界原点，不要再额外上移；逻辑层 `y=0` 会自然满足底面 y=0、顶面 y=1。

坐标换算关系：

```text
方块逻辑坐标 (x, y, z)
方块中心世界坐标 (x + 0.5, y + 0.5, z + 0.5)  // GridMap 默认三轴居中
方块垂直范围 [y, y + 1]
```

当前 GridMap 默认以单元中心排布。关键是全项目只通过 `local_to_map()` / `map_to_local()` 换算，不手写浮点取整规则。如果未来关闭 `cell_center_x/y/z`，则必须同步调整方块 Mesh 偏移、射线换算和存档坐标约定。

## 2. 数据结构：逻辑数据必须独立于画面节点

### 2.1 方块类型

使用整数 ID，不在每一格里存字符串或 Resource：

```gdscript
enum BlockType {
    AIR = -1,
    GRASS = 0,
    DIRT = 1,
    STONE = 2,
    PLANKS = 3,
}
```

整数 ID 体积小、比较快、方便序列化。名称、贴图、硬度等共享属性放在方块注册表中，通过 ID 查询。

### 2.2 当前 Demo：GridMap 直接保存占用信息

当前项目的 `VoxelWorld` 继承 `GridMap`，每个 `Vector3i` 单元记录一个 MeshLibrary item ID：

```gdscript
var block_type: int = world.get_block(Vector3i(x, y, z))
world.place_block(Vector3i(x, y, z), VoxelWorld.GRASS)
world.remove_block(Vector3i(x, y, z))
```

优点：

- Godot 原生提供整数网格坐标。
- 自动批处理同类网格，远好于 62,500 个 MeshInstance3D 节点。
- 自动生成方块碰撞，射线可直接命中 GridMap。
- 代码少，适合验证移动、破坏、放置的完整闭环。

限制：GridMap 同时承担了“数据、渲染、碰撞”三种职责。世界进一步扩大、出现多层洞穴或频繁编辑时，存档、流式加载和网格优化会受到限制。

### 2.3 正式版推荐：Chunk + PackedByteArray

把世界按 `16 × 16 × 16` 或 `16 × 32 × 16` 分块。每个 Chunk 保存一段连续的一维数组：

```gdscript
const CHUNK_SIZE := Vector3i(16, 16, 16)
var blocks := PackedByteArray()

func index_of(local: Vector3i) -> int:
    return local.x + CHUNK_SIZE.x * (local.z + CHUNK_SIZE.z * local.y)
```

如果方块类型不超过 255 种，`PackedByteArray` 每格只需 1 字节。一个 `16³` Chunk 的裸方块数据约 4KB。若未来超过 255 种，可改为 `PackedInt32Array`，或将方块 ID 与状态拆分。

世界级索引使用：

```gdscript
var chunks: Dictionary[Vector3i, VoxelChunkData]
```

Key 是 Chunk 坐标，Value 是 Chunk 数据。世界坐标先转换为 Chunk 坐标和 Chunk 内局部坐标。这样可以：

- 只加载玩家附近区块。
- 修改一个方块时只重建所在 Chunk；若位于边界，再重建相邻 Chunk。
- 每个 Chunk 单独存档、压缩和校验。
- 将逻辑数据与 MeshInstance3D、碰撞体完全分离。

对于当前 250×250 单层世界，也可使用二维 `PackedByteArray`：

```gdscript
var ground := PackedByteArray()
ground.resize(250 * 250)
var index := (z - MIN_Z) * 250 + (x - MIN_X)
```

但一旦允许玩家向上堆叠或向下挖掘，二维数组就不够自然，因此正式架构直接使用三维 Chunk 更稳妥。

## 3. 初始生成流程

当前 Demo 的生成步骤：

1. `Main` 创建 `VoxelWorld`。
2. `VoxelWorld._ready()` 配置 1 米单元、MeshLibrary、碰撞层和选中框。
3. `build_initial_world()` 清空旧数据。
4. 双层循环遍历 `x=-125..124`、`z=-125..124`。
5. 在 `Vector3i(x, 0, z)` 写入 `GRASS`。
6. GridMap 根据单元数据生成可见网格与碰撞。
7. 生成结束后再创建玩家，出生点位于地面上方。

伪代码：

```gdscript
for x in range(-125, 125):
    for z in range(-125, 125):
        set_block(Vector3i(x, 0, z), GRASS)
```

250×250 只有 62,500 次写入，可在加载阶段一次完成。若后续生成多层地形，应分 Chunk 构建，必要时每帧处理少量区块，避免首帧卡顿。

## 4. 场景树组织

### 当前可运行 Demo

```text
HengHengLittleWorld (Node3D)
├── WorldEnvironment
├── DirectionalLight3D
├── VoxelWorld (GridMap)
│   └── BlockHighlight (MeshInstance3D)
├── Player (CharacterBody3D)
│   ├── CollisionShape3D
│   └── Camera3D
└── HUD (CanvasLayer)
```

注意：场景树里只有一个 GridMap，**不会有 62,500 个方块节点**。不要给每个方块创建 `StaticBody3D + MeshInstance3D + CollisionShape3D`，否则节点数量、Draw Call、物理对象和内存开销都会过高。

### 正式 Chunk 架构

```text
WorldRoot (Node3D)
├── ChunkManager (Node)
├── Chunk_(-8,0,-8) (Node3D)
│   ├── MeshInstance3D
│   └── StaticBody3D
│       └── CollisionShape3D
├── Chunk_(-7,0,-8)
│   ├── MeshInstance3D
│   └── StaticBody3D
└── ...
```

每个 Chunk 通常只需要一个可见网格和一个合并碰撞，而不是每格一个节点。

## 5. 是否需要实例化或网格合并

### 不推荐：逐方块实例化节点

即使使用相同 PackedScene，实例化 62,500 个节点仍会造成：

- 场景树遍历和内存开销大。
- 物理碰撞对象过多。
- 方块编辑时节点管理复杂。
- 大量不可见内部面仍被提交。

### 当前 Demo：GridMap 足够

对于 250×250 单层平地和少量玩家编辑，GridMap 是合理的最小方案。它共享 MeshLibrary，并比逐块节点更适合原型。

### 正式版：必须按 Chunk 合并网格

推荐每个 Chunk 用 `ArrayMesh` 或 `SurfaceTool` 生成一个 Mesh，只输出暴露在空气旁边的面：

- 顶面：上方为空气才生成。
- 底面：下方为空气才生成。
- 四个侧面：对应邻格为空气才生成。

单层 250×250 平地若逐方块生成完整立方体，需要 375,000 个面；经过邻面剔除后，只需要 62,500 个顶面、62,500 个底面和外围 1,000 个侧面，共约 126,000 个四边形。若底面永远不可见且不可挖，还可暂时不生成底面。

进一步使用 Greedy Meshing，可以把相邻、同材质、同朝向的面合并成大矩形。纯平、单一草方块的整个 Chunk 顶面理论上可合并成极少量四边形。但纹理需要设置重复采样或在 Shader 中按世界坐标取样，避免一张草纹理被拉伸。

碰撞也应按 Chunk 合并：

- 可编辑体素：由 Chunk 网格生成三角形碰撞，Chunk 修改后重建。
- 当前纯平地：可额外用一个大 BoxShape3D 做地面碰撞，但一旦允许挖洞就必须改回与方块数据一致的 Chunk 碰撞。

## 6. 建议的演进边界

当前阶段保留以下稳定接口：

```gdscript
get_block(cell: Vector3i) -> int
has_block(cell: Vector3i) -> bool
place_block(cell: Vector3i, block_type: int) -> bool
remove_block(cell: Vector3i) -> bool
```

玩家射线和 HUD 只依赖这些接口，不直接依赖 GridMap 内部实现。以后把 VoxelWorld 从 GridMap 改为 ChunkManager 时，玩家交互逻辑无需重写。

建议顺序：

1. 当前阶段：250×250 GridMap，验证玩法。
2. 加存档前：抽出 `WorldData`，数据不再以 GridMap 为唯一来源。
3. 世界增加高度前：实现 16³ Chunk 数据。
4. 世界扩大或加入复杂地形前：实现邻面剔除和 Chunk Mesh。
5. 性能确有压力后：加入 Greedy Meshing、后台生成和视距加载。

不要一开始就实现无限世界和复杂贪心网格；先固定数据接口与坐标约定，能显著降低后续替换成本。
