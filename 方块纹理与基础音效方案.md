# 方块纹理与基础音效方案

## 1. 本阶段目标

为《哼哼的小小世界》的五种基础方块接入独立像素纹理，并为破坏、放置操作增加两种不同的空间音效。

当前实现继续采用适合原型期的 `GridMap + MeshLibrary`：每一种方块对应一个 `BoxMesh` 和一个 `StandardMaterial3D`。音效由独立的 `BlockAudioManager` 监听世界数据事件，不与输入或玩家控制代码耦合。

## 2. 资产目录

```text
assets/
├── textures/blocks/
│   ├── grass.png       # 草方块，绿色像素斑点
│   ├── dirt.png        # 泥土，深浅棕色颗粒
│   ├── stone.png       # 石头，灰色矿物斑块
│   ├── planks.png      # 木板，横向木纹与节疤
│   └── bricks.png      # 砖块，错列砖缝与浅色灰浆
└── audio/sfx/
    ├── block_break.wav # 破坏：碎裂噪声、脆响与低频冲击
    └── block_place.wav # 放置：短促结实的敲击声
```

纹理均为 16×16 RGBA PNG，音效均为 22050Hz、16-bit、单声道 WAV。`tools/generate_block_assets.py` 可以确定性地重新生成全部资产，便于后续统一调色和调音。

## 3. 当前材质方案：独立 StandardMaterial3D

每种方块在 `VoxelWorld._create_block_library()` 中创建一个 1 米 `BoxMesh`，通过 `StandardMaterial3D.albedo_texture` 加载相应 PNG：

```gdscript
var texture := load(texture_path) as Texture2D
var material := StandardMaterial3D.new()
material.albedo_texture = texture
material.texture_filter = BaseMaterial3D.TEXTURE_FILTER_NEAREST
material.roughness = 0.92
```

### 优点

- 实现直接，方块类型和贴图是一对一关系。
- 单张纹理可独立替换、调色和检查。
- 与当前 `GridMap + MeshLibrary` 结构完全匹配。
- 不需要手动计算 Atlas UV，原型阶段更不容易出错。

### 缺点

- 每一种材质通常对应独立渲染批次。
- 方块种类增多后，材质切换和 Draw Call 会增长。
- 不适合未来将大量可见方块合并为单个 Chunk 网格的目标。

## 4. Atlas 方案与迁移时机

纹理 Atlas 是把多种方块纹理合并到同一张大图中，例如使用 64×64 Atlas 容纳多张 16×16 图块，再让每个方块面的 UV 指向对应区域。

### 优点

- 多种方块可以共用同一个材质。
- 与 Chunk 网格合并、邻面剔除和 Greedy Meshing 配合后，可显著减少 Draw Call。
- 更适合几十种乃至数百种方块的正式版本。

### 缺点

- 生成网格时必须为每个面计算正确 UV。
- Mipmap、纹理过滤和边缘采样不当时容易产生纹理串色。
- 方块新增、动画纹理和不同面贴图会提高 Atlas 管理复杂度。

### 建议迁移边界

当前五种方块、GridMap 原型继续使用独立 `StandardMaterial3D`。当世界渲染迁移到 16×16×16 Chunk、自定义 `ArrayMesh`、邻面剔除或 Greedy Meshing 时，同步迁移为 Atlas。此时应：

1. 给每个方块面写入 Atlas UV。
2. 为每个图块保留 1–2 像素内边距，或在 Atlas 中复制边缘像素。
3. 使用最近邻过滤；像素风项目可关闭 mipmap，远景闪烁明显时则生成带 padding 的 mipmap。
4. Chunk 内所有不透明方块共用一个材质，透明方块另建渲染表面。

## 5. 像素纹理设置

- 纹理分辨率：基础方块采用 16×16，与经典像素方块风格一致。
- 映射比例：每个 1 米方块的每个面完整显示一张纹理。
- 过滤方式：`TEXTURE_FILTER_NEAREST`，避免像素边缘被双线性过滤变糊。
- 材质粗糙度：`0.92`，让草、土、石、木和砖保持低反射的哑光外观。
- 色彩空间：颜色纹理按 sRGB 导入，法线图等数据纹理未来应使用线性空间。
- Mipmap：当前近距离小世界可以不依赖 mipmap；未来视距增大时应结合 Atlas padding 评估开启。

## 6. 音效节点组织

运行时场景树：

```text
HengHengLittleWorld
├── DayNightCycle
├── VoxelWorld
├── BlockAudioManager
│   ├── BreakPlayer0..3
│   └── PlacePlayer0..3
├── Player
└── HUD
```

`BlockAudioManager` 是独立 `Node3D`，内部建立两个播放器池：

- 4 个破坏 `AudioStreamPlayer3D`。
- 4 个放置 `AudioStreamPlayer3D`。

每个播放器设置有限的 `max_distance`，声音在发生方块变化的世界坐标播放。这样玩家能根据声源方向感知操作位置。

## 7. 音效触发时机

音效不直接由鼠标输入触发，因为一次输入可能因无目标、已有方块或玩家重叠而失败。管理器只监听成功的数据变化：

```gdscript
func _on_block_changed(cell: Vector3i, previous_type: int, new_type: int) -> void:
    if previous_type != VoxelWorld.AIR and new_type == VoxelWorld.AIR:
        _play_break(cell)
    elif previous_type == VoxelWorld.AIR and new_type != VoxelWorld.AIR:
        _play_place(cell)
```

因此：

- 成功破坏才播放破坏声。
- 成功放置才播放放置声。
- 被规则拒绝的操作不会产生错误音效。
- 所有未来系统（玩家、NPC、脚本事件）只要调用统一 `set_block()`，都会得到一致声音反馈。

初始 62,500 个地面方块通过批量 `set_cell_item()` 生成，不会触发 62,500 次音效。

## 8. 防止声音异常重叠

仅使用一个播放器会导致连续点击时前一个声音被 `play()` 截断；每次操作无限创建节点又会造成叠音失控和节点增长。因此采用固定池：

1. 优先选择没有播放的播放器。
2. 两类声音各自最多 4 路并发。
3. 池满时停止并轮转复用最旧槽位，不创建新节点。
4. 每次播放加入轻微 `pitch_scale` 随机变化，减少机械重复感。
5. 破坏和放置使用不同播放器池，互不抢占。

后续可以增加每类方块的声音集合，例如石头、木板、泥土分别拥有 3–5 个变体，并按方块类型选择音效组；固定池策略仍可保持不变。
