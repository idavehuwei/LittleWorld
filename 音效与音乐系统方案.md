# 《哼哼的小小世界》音效与音乐系统方案

## 1. Audio Bus 组织

```text
Master
├── Music
└── SFX
    └── Ambient
```

- `Master`：最终总输出；“主音量”滑块控制。
- `Music`：背景音乐；“音乐音量”滑块控制。
- `SFX`：方块、脚步及环境音的总父级；“音效音量”滑块控制。
- `Ambient`：风声和鸟鸣，发送到 `SFX`，因此既可以统一受音效音量控制，未来也可单独添加环境音设置。

本阶段由 `GameAudioSystem.configure_audio_buses()` 在启动时保证总线存在，避免手工维护二进制或复杂的总线资源文件。

## 2. 节点职责

```text
Main
├── VoxelWorld
├── BlockAudioManager (Node3D)
│   ├── BreakPlayer0..3 (AudioStreamPlayer3D)
│   └── PlacePlayer0..3 (AudioStreamPlayer3D)
├── Player (CharacterBody3D)
│   └── Footsteps (FootstepController)
│       └── FootstepPlayer (AudioStreamPlayer3D)
├── GameAudioSystem (Node)
│   ├── BackgroundMusic (AudioStreamPlayer)
│   ├── WindAmbience (AudioStreamPlayer)
│   └── BirdAmbience (AudioStreamPlayer)
└── HUD
    └── InventoryPanel
        └── AudioSettings
```

- 方块破坏/放置具有世界位置和距离衰减，使用 `AudioStreamPlayer3D` 固定池。
- 脚步声来自玩家位置，播放器直接挂在玩家下。
- BGM、风声和鸟鸣属于不需要空间定位的整体氛围，使用 `AudioStreamPlayer`。
- 三档音量滑块放在背包面板内，玩家按 E 打开背包即可调整。

## 3. 方块材质音效映射

方块按听感归为三组：

```text
grass：草、泥土、树叶、花朵及默认软质方块
stone：石头、砖块
wood：原木、木板
```

每组都有独立的破坏和放置 WAV：

```text
break_grass.wav / place_grass.wav
break_stone.wav / place_stone.wav
break_wood.wav  / place_wood.wav
```

`BlockAudioManager` 监听成功的 `world.block_changed`，破坏时根据 `previous_type` 选择音效，放置时根据 `new_type` 选择音效。被规则拒绝的交互不会改变世界，也不会错误播放声音。

播放器池每类固定四路。优先找空闲播放器；全部占用时停止并轮转复用最旧槽位，避免快速操作无限创建节点。每次播放加入小幅 `pitch_scale` 随机变化，减少机械重复感。

## 4. 动态脚步声

`FootstepController` 每个物理帧记录玩家在 X/Z 平面的实际位移，仅当玩家：

- 已落地；
- 水平速度大于最低阈值；
- 累计移动距离达到当前步距；

才播放脚步声。

脚下方块查询：

```gdscript
var local_position := world.to_local(
    player.global_position + Vector3(0.0, -0.08, 0.0)
)
var cell := world.local_to_map(local_position)
var block_type := world.get_block(cell)
```

如果第一格为空气，则向下再查询一格。随后调用与方块音效相同的草/石/木分组逻辑，动态设置 `FootstepPlayer.stream`。

步频采用“累计距离”而不是固定计时器：

```text
慢速：约每 2.35 米一步
全速：约每 1.60 米一步
```

因此玩家移动越快，单位时间内跨过的步距越多，脚步自然更频繁；停止、腾空或落下时不会持续播放脚步。

## 5. 环境音与背景音乐

- `WindAmbience`：8 秒轻量风声，使用 WAV 前向循环，音量较低。
- `BirdAmbience`：非循环鸟鸣，每隔 8–18 秒随机播放一次，并轻微随机变调。
- `BackgroundMusic`：16 秒舒缓旋律与和弦垫底，前向循环；播放器本身为低音量，默认 `Music` 总线也仅约 38%。

循环资源使用 `AudioStreamWAV.LOOP_FORWARD`，而不是每次播放完重新调用 `play()`，避免循环接缝处出现额外调度间隔。

## 6. 音量设置

背包中的音量区包含三个 `HSlider`：

```text
主音量：Master，默认 82%
音乐：Music，默认 38%
音效：SFX，默认 78%
```

滑块值为 0–100，运行时转为 0–1 线性值，再用 `linear_to_db()` 写入 AudioServer。0% 时直接静音对应总线，避免对 0 求对数。

当前设置在本次运行期间有效。正式存档阶段可将三个线性值保存到 `user://audio_settings.cfg`，启动时读取后覆盖默认值。

## 7. 资产与扩展

音频由 `tools/generate_block_assets.py` 使用 Python 标准库确定性生成，可重复构建：

- 3 组破坏音效。
- 3 组放置音效。
- 3 组脚步音效。
- 风声和鸟鸣。
- 一首循环背景音乐。

后续替换专业素材时保持现有路径和总线分组即可，无需修改调用方。若加入水、沙、雪、金属等材质，只需扩展 `sound_group_for_block()`、`surface_group_for_block()` 和对应流字典。
