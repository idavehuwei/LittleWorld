# 《哼哼的小小世界》世界存档与读档系统方案

## 1. 存档格式选择

当前阶段采用**版本化 JSON 单槽存档**：

```text
user://save_slot_1.json
```

JSON 的优势：

- 人类可读，方便调试方块坐标、物品数量和版本迁移。
- Godot 原生 `JSON.stringify()` / `JSON.parse()`，不依赖第三方库。
- 当前只保存世界差异，文件体积远小于完整 250×250 世界。
- 增加新字段时可以使用默认值保持向后兼容。

不使用 `Resource` 的主要原因是存档属于用户运行数据而非项目资源，且自定义 Resource 需要维护脚本类和资源兼容性；暂不使用二进制，是因为当前单槽数据量不大，JSON 更容易排查问题。未来世界差异达到几十万项时，可保留同一逻辑结构，改用 `FileAccess.store_var()` 或压缩二进制 Chunk 数据。

## 2. 单槽数据结构

```json
{
  "version": 1,
  "world": {
    "terrain_seed": 20260731,
    "modified_blocks": [
      {"x": 10, "y": 1, "z": 5, "type": -1},
      {"x": 11, "y": 1, "z": 5, "type": 2}
    ]
  },
  "player": {
    "position": [0.5, 2.0, 5.5],
    "body_rotation_y": 0.0,
    "head_rotation_x": 0.0
  },
  "inventory": {
    "selected_hotbar_index": 0,
    "slots": [
      {"item": 0, "amount": 64}
    ]
  },
  "day_night": {
    "time_of_day": 0.333333
  }
}
```

方块类型 `-1` 表示该初始方块已被破坏为空气。玩家放置的新方块则记录对应方块 ID。

## 3. 为什么只保存修改方块

自然世界由固定 `TERRAIN_SEED` 确定性生成，因此读档时无需保存和重写全部地形：

```text
按固定种子重建初始世界
→ 应用 modified_blocks 差异
→ 恢复玩家、背包和昼夜
```

`VoxelWorld.modified_blocks` 仅在 `tracking_changes` 开启后记录 `set_block()` 成功变更。初始地形构建期间关闭跟踪，避免将几十万基础方块写入存档。

这种方案当前最简单、文件最小。以后若支持多个世界种子，只需把存档中的 `terrain_seed` 注入世界生成器，而不是使用常量。

## 4. 保存流程

`SaveGameManager` 持有：

- `VoxelWorld`
- `FirstPersonPlayer`
- `PlayerInventory`
- `DayNightCycle`

保存时：

```text
收集世界差异
→ 收集玩家位置和身体/头部旋转
→ 收集36格物品和快捷栏索引
→ 收集time_of_day
→ JSON.stringify
→ 写入 user://save_slot_1.tmp
→ flush + close
→ 将旧存档临时改名为 save_slot_1.bak
→ 将临时文件替换为 save_slot_1.json
→ 成功后删除备份；失败则回滚旧存档
```

先写临时文件再替换正式文件，避免写入中途退出导致原存档只剩半个 JSON。当前是单线程同步写入，因为差异数据很小；大型存档可将 JSON/二进制编码移到工作线程，但最终文件替换仍应在完成后执行。

## 5. 手动与自动保存

- F5：`SaveGameManager._unhandled_input()` 调用 `save_game("manual")`。
- 自动保存：内部倒计时 `300.0` 秒，到期调用 `save_game("auto")`，成功后重置五分钟；写入失败则在 30 秒后重试。
- 读档和开始新游戏后都重置自动保存倒计时。

输入映射仍在 `main.gd` 代码注册：

```gdscript
_bind_key(&"save_game", KEY_F5)
```

## 6. 启动流程

主场景完成世界、玩家、背包和系统装配后检查：

```gdscript
if save_game_manager.has_save_file():
    _show_start_menu()
```

### 无存档

直接保留已经生成的新世界，玩家可立即开始游戏，不显示额外界面。

### 有存档

暂停整个 `SceneTree` 并释放鼠标；启动菜单设置为 `PROCESS_MODE_WHEN_PAUSED`，因此只有菜单在暂停期间继续处理输入。界面显示：

```text
继续游戏
开始新游戏
```

- “继续游戏”：解析并校验 JSON，重建初始世界，应用差异，再恢复玩家、背包和昼夜。
- “开始新游戏”：删除单槽文件，重建自然世界，恢复演示背包、默认出生点和 8:00 昼夜时间。

菜单关闭后解除全局暂停并恢复第一人称鼠标捕获，动物、昼夜、玩家物理和自动保存从同一时刻继续运行。

## 7. 加载顺序与安全检查

加载顺序必须是：

```text
验证version
→ 重建固定种子世界
→ 应用方块差异
→ 恢复玩家变换并清零速度
→ 恢复36格背包和快捷栏选择
→ 恢复昼夜时间
→ 重置自动保存计时
```

安全规则：

- JSON 解析失败时不修改当前运行状态。
- 版本不支持时拒绝加载。
- 方块条目必须包含 x/y/z/type，类型必须为空气或已注册方块。
- 背包物品必须存在于 `ItemCatalog`，数量按堆叠上限截断。
- 玩家垂直视角限制在 ±89°。
- 缺失字段使用安全默认值。

## 8. 代码职责

- `scripts/save_game_manager.gd`：单槽文件、JSON、手动/自动保存、状态收集和恢复。
- `scripts/voxel_world.gd`：跟踪、序列化和应用修改方块。
- `scripts/inventory.gd`：序列化/恢复全部36格及快捷栏选择。
- `scripts/main.gd`：F5输入注册、存档管理器装配、启动选择界面。
- `test/test_save_game.gd`：使用独立测试路径验证保存和恢复，不污染正式存档。
