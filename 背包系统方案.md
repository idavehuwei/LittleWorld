# 《哼哼的小小世界》背包系统方案

## 1. 系统目标

玩家拥有固定 36 格背包：

- 第 0–8 格：9 格快捷栏，始终显示在屏幕底部。
- 第 9–35 格：27 格主背包，仅在按 E 打开背包时显示。
- 每个槽位保存“物品类型 + 数量”。
- 基础方块最大堆叠数量为 64。
- 数字键 1–9 和鼠标滚轮切换当前快捷栏槽位。
- 成功破坏方块会把方块收入背包；背包无法容纳时不破坏方块。
- 成功放置方块会从当前快捷栏槽位消耗 1 个物品。

当前版本在演示开始时给前五个快捷栏槽位各放入 64 个草、泥土、石头、木板和砖块，后四格为空。

## 2. 运行时结构

```text
HengHengLittleWorld
├── DayNightCycle
├── VoxelWorld
├── BlockAudioManager
├── Player
│   ├── BodyCollision
│   └── Head
│       └── Camera3D
└── HUD
    ├── Crosshair
    ├── Hotbar          # 9 格，始终可见
    └── InventoryPanel  # 9×4=36 格，按 E 显示
```

数据对象不是 UI 节点：

```text
PlayerInventory (RefCounted)
├── item_ids: PackedInt32Array[36]
├── amounts: PackedInt32Array[36]
└── selected_hotbar_index: int
```

`Main` 创建唯一的 `PlayerInventory`，再把同一个引用注入 `Player` 和 `HUD`，保证玩法和界面读取的是同一份数据。

## 3. 物品定义

`ItemCatalog` 作为物品定义目录，集中提供：

```gdscript
static func is_valid_item(item_id: int) -> bool
static func display_name(item_id: int) -> String
static func icon_path(item_id: int) -> String
static func stack_limit(item_id: int) -> int
```

当前方块物品直接复用 `VoxelWorld` 的方块 ID：

| ID | 物品 | 图标 |
|---:|---|---|
| 0 | 草方块 | `grass.png` |
| 1 | 泥土 | `dirt.png` |
| 2 | 石头 | `stone.png` |
| 3 | 木板 | `planks.png` |
| 4 | 砖块 | `bricks.png` |

空气 `-1` 同时表示空槽。基础方块的 `stack_limit()` 返回 64。未来若加入工具、武器或食物，可让目录针对不同物品返回 1、16、64 等不同上限。

正式项目中还可把定义迁移为自定义 `Resource`：

```gdscript
class_name ItemDefinition
extends Resource

@export var id: int
@export var display_name: String
@export var icon: Texture2D
@export var max_stack: int = 64
@export var placeable_block_id: int = -1
```

这样内容人员可在编辑器里新增物品，而无需修改代码。

## 4. 槽位数据结构

当前 36 格容量固定，使用两个紧凑的 `PackedInt32Array`：

```gdscript
item_ids[index] # 物品类型，-1 表示空槽
amounts[index]  # 数量，空槽必须为 0
```

这种“平行数组”方案有以下优点：

- 固定长度，索引和 UI 格子天然一一对应。
- 保存时可直接序列化两个整数数组。
- 不为每个空槽创建 Dictionary 或对象。
- GDScript 类型明确，减少 Variant 类型推断问题。

系统保持不变量：

```text
item_id == AIR  <=>  amount == 0
0 < amount <= stack_limit(item_id)
```

## 5. 堆叠与添加算法

`add_item(item_id, amount)` 分两阶段处理：

1. 从第 0 格开始查找同种物品且未达到 64 的堆栈，优先补满。
2. 若仍有剩余，再按顺序查找空槽，每个空槽最多放 64。
3. 返回未能放入的剩余数量；返回 0 代表全部成功。

伪代码：

```gdscript
remaining = amount
for 同类未满槽:
    moved = min(remaining, 64 - slot.amount)
    slot.amount += moved
    remaining -= moved
for 空槽:
    moved = min(remaining, 64)
    slot.item_id = item_id
    slot.amount = moved
    remaining -= moved
return remaining
```

`can_add()` 使用相同容量规则但不修改数据，可用于破坏方块前的预检查。

## 6. 破坏方块事务

破坏操作不能先删世界方块再尝试入包，否则背包已满时会造成物品丢失。正确顺序为：

```text
检查有高亮目标
    ↓
读取目标方块类型
    ↓
Inventory.can_add(type, 1)
    ↓ 否
保留世界方块，不播放破坏音效
    ↓ 是
World.remove_block(cell)
    ↓
Inventory.add_item(type, 1)
```

当前按需求中的可选行为选择“背包已满则不拾取，并且不破坏方块”。这样无需立刻引入掉落实体，也不会让资源凭空消失。

未来实现地面掉落物时，可以改为：世界方块成功移除后，若 `add_item()` 有剩余，则在方块中心生成 `DroppedItem3D`，玩家靠近后再次尝试拾取。

## 7. 放置方块事务

放置时物品类型来自当前快捷栏槽位，而不是独立的 `selected_block` 列表：

```text
读取 selected_hotbar_index
    ↓
检查槽位非空且数量 > 0
    ↓
检查相邻格为空且不与玩家重叠
    ↓
World.place_block(cell, selected_item)
    ↓ 成功
Inventory.remove_from_slot(index, 1)
```

必须在世界写入成功后才扣除物品，避免碰撞拒绝或目标已占用时错误消耗。数量变为 0 时，槽位自动恢复为空：

```gdscript
amounts[index] -= 1
if amounts[index] == 0:
    item_ids[index] = AIR
```

空快捷栏槽位不能放置方块。

## 8. 快捷栏 UI

屏幕底部的 `HBoxContainer` 始终创建 9 个槽位，每格包含：

- `TextureRect`：加载物品对应的 16×16 方块纹理，最近邻显示。
- `Label`：右下角显示数量。
- `Label`：左上角显示数字键 1–9。
- `Button`：提供可点击区域。
- `StyleBoxFlat`：普通格为灰色边框，当前选中格为黄色粗边框。

无论格子是否为空，9 格位置始终稳定，不会因物品数量变化造成布局跳动。

## 9. 完整背包界面

`InventoryPanel` 使用居中的 `PanelContainer`，内部 `GridContainer.columns = 9`，按 9×4 展示全部 36 格：

- 第一行：快捷栏 9 格。
- 第二至第四行：主背包 27 格。

当前实现支持点击两个格子交换内容：第一次点击记录源槽位，第二次点击执行 `swap_slots()`。这为把主背包物品移动到快捷栏提供了最小但完整的鼠标交互。

按 E 打开时：

```gdscript
inventory_panel.visible = true
crosshair.visible = false
Input.mouse_mode = Input.MOUSE_MODE_VISIBLE
```

按 E 关闭时：

```gdscript
inventory_panel.visible = false
crosshair.visible = true
Input.mouse_mode = Input.MOUSE_MODE_CAPTURED
```

界面打开期间玩家停止水平输入、不能转动视角、不能破坏或放置，但重力仍继续处理，避免在半空打开界面后角色悬停。

## 10. UI 与数据同步

`PlayerInventory` 发出三个信号：

```gdscript
signal slot_changed(index: int)
signal inventory_changed
signal selection_changed(index: int)
```

同步策略：

- 单个槽位变化时，HUD 只刷新对应快捷栏格和背包格。
- 选择变化时，只更新黄色高亮边框和“当前物品”文字。
- HUD 不直接修改数组，所有变更必须调用背包公开方法。
- 玩家不直接操作 UI，只读背包当前槽位并调用增减接口。

这种单向数据流避免“快捷栏显示 10 个，但数据实际只有 9 个”的状态分裂：

```text
输入/玩法 → PlayerInventory → signals → HUD refresh
```

## 11. 保存和扩展

存档时只需要保存：

```json
{
  "item_ids": [0, 1, 2, 3, 4, -1, ...],
  "amounts": [64, 64, 64, 64, 64, 0, ...],
  "selected_hotbar_index": 0
}
```

加载后应验证：数组长度必须为 36、物品 ID 必须存在、数量必须在合法堆叠范围内。不要直接信任外部存档数据。

后续可在不改变核心结构的前提下扩展：

- Shift+点击快速在快捷栏和主背包间移动。
- 左键整组拿起、右键单个分配。
- 地面掉落物和自动拾取。
- 箱子等双容器界面。
- 工具耐久度和不可堆叠物品。
- 创造模式无限物品栏。
