# 《哼哼的小小世界》地形生成与自然装饰方案

## 1. 目标与范围

世界保持为 250×250 个水平坐标格，坐标范围：

```text
x = -125..124
z = -125..124
```

地表不再统一位于 `y=0`，而是由 `FastNoiseLite` 生成整数高度，限制在：

```text
MIN_SURFACE_HEIGHT = -3
MAX_SURFACE_HEIGHT = +5
```

每个水平坐标柱从上到下由草、泥土和石头组成，地形统一填充至 `WORLD_BOTTOM_Y = -8`。地表额外生成树木与花朵，出生点 `(0, 5)` 周围保持平坦且没有装饰遮挡。

## 2. 数据结构

`VoxelWorld` 继续使用 `GridMap + MeshLibrary` 保存实际世界方块，同时增加一个长度为 62,500 的一维高度图：

```gdscript
var height_map := PackedInt32Array()
```

索引采用行主序：

```gdscript
index = local_z * WORLD_WIDTH + local_x
```

高度图只保存每列的地表 Y 坐标。完整方块仍由 GridMap 管理，所以原有射线、碰撞、破坏、放置和 `block_changed` 接口不需要改写。

公开查询：

```gdscript
get_surface_height(x: int, z: int) -> int
spawn_world_position() -> Vector3
```

## 3. FastNoiseLite 配置

项目创建两套噪声：

```gdscript
terrain_noise = FastNoiseLite.new()
detail_noise = FastNoiseLite.new()
```

### 主地形噪声

```gdscript
terrain_noise.seed = 20260731
terrain_noise.noise_type = FastNoiseLite.TYPE_SIMPLEX_SMOOTH
terrain_noise.frequency = 0.018
terrain_noise.fractal_type = FastNoiseLite.FRACTAL_FBM
terrain_noise.fractal_octaves = 4
terrain_noise.fractal_lacunarity = 2.0
terrain_noise.fractal_gain = 0.5
```

- 低频率产生宽缓丘陵，避免相邻方块高度剧烈跳变。
- FBM 叠加多个八度，让大地形中带有中等尺度细节。
- 固定种子确保自动化测试、多人同步和存档重建时结果一致。

`get_noise_2d()` 返回约 `-1..1`，先归一化到 `0..1`，再映射到高度范围：

```gdscript
var normalized := clampf((noise_value + 1.0) * 0.5, 0.0, 1.0)
var raw_height := roundi(lerpf(-3.0, 5.0, normalized))
```

最终使用整数高度，保持所有方块严格对齐 1 米网格。

### 泥土深度噪声

第二套较高频噪声用于决定每列是 1、2 还是 3 层泥土：

```gdscript
var detail := detail_noise.get_noise_2d(float(x), float(z))
var dirt_depth := clampi(2 + roundi(detail), 1, 3)
```

这样地层厚度不会完全机械一致。

## 4. 出生区平整

只把出生点单个坐标改为平地会形成突兀断崖，因此采用“内圈完全压平、外圈平滑混合”：

```text
距离出生点 ≤ 10 格：高度固定为 0
10–18 格：从 0 平滑过渡到噪声高度
距离 > 18 格：使用完整噪声高度
```

实现：

```gdscript
var distance := Vector2(x - spawn_x, z - spawn_z).length()
var blend := smoothstep(10.0, 18.0, distance)
var final_height := roundi(lerpf(0.0, float(raw_height), blend))
```

树木和花朵也禁止生成在出生点 18 格内。玩家位置通过 `spawn_world_position()` 获取，避免主场景硬编码 Y 坐标。

## 5. 按高度分层填充

对每个 `(x, z)`：

```text
surface_y               → 草方块
surface_y - 1..-N       → 泥土，N为1–3
其余直到 WORLD_BOTTOM  → 石头
```

伪代码：

```gdscript
set_cell_item(Vector3i(x, surface_y, z), GRASS)

for layer in range(1, dirt_depth + 1):
    set_cell_item(Vector3i(x, surface_y - layer, z), DIRT)

for y in range(surface_y - dirt_depth - 1, WORLD_BOTTOM_Y - 1, -1):
    set_cell_item(Vector3i(x, y, z), STONE)
```

生成阶段直接使用 `set_cell_item()`，避免为十多万个初始方块逐个发出 `block_changed`。游戏运行期间的玩家操作仍统一走 `set_block()`。

## 6. 树木生成

使用固定种子的 `RandomNumberGenerator` 扫描地表。每个候选格满足以下条件才生成：

1. 距离出生点超过 18 格。
2. 不靠近世界边缘。
3. 3×3 邻域地表高差不超过 1 格。
4. 树干和树冠位置没有其他装饰。
5. 随机值低于 `TREE_CHANCE`。

树干：

```gdscript
trunk_height = rng.randi_range(3, 5)
```

从地表上方连续放置 3–5 个 `LOG`。原木复用背包木头 ID `100`，破坏后可以直接用于“1 木头→4 木板”配方。

树冠：

- 树干顶部附近生成两层 3×3 树叶。
- 顶部增加中心与四个方向的十字叶片。
- 树干中心位置优先保留原木。

树叶使用实体方块碰撞，玩家不能穿过树冠。

## 7. 花朵生成

花朵只在草方块上方空气格生成，并避开出生安全区和树木占用位置。

视觉上使用一个 `ArrayMesh`，包含互相垂直的两个平面：

```text
平面 A：沿 X 方向
平面 B：沿 Z 方向
```

材质设置：

```gdscript
transparency = BaseMaterial3D.TRANSPARENCY_ALPHA_SCISSOR
alpha_scissor_threshold = 0.35
cull_mode = BaseMaterial3D.CULL_DISABLED
texture_filter = BaseMaterial3D.TEXTURE_FILTER_NEAREST
```

`flower.png` 是 16×16 透明像素纹理。花朵不注册物理形状，玩家可穿过，射线仍可通过 GridMap 单元选中和破坏。

## 8. 方块与物品扩展

新增世界方块：

```gdscript
VoxelWorld.LEAVES = 5
VoxelWorld.FLOWER = 6
VoxelWorld.LOG = 100
```

纹理：

```text
assets/textures/blocks/log.png
assets/textures/blocks/leaves.png
assets/textures/blocks/flower.png
```

树叶和花朵已加入 `ItemCatalog`，破坏后可进入背包。原木与已有合成系统共用同一个物品 ID。

## 9. 生成顺序

```text
配置 FastNoiseLite
    ↓
生成 62,500 个地表高度样本
    ↓
应用出生区压平与过渡
    ↓
逐列填充草/泥土/石头
    ↓
确定性随机扫描树木候选
    ↓
生成花朵
    ↓
根据高度图设置玩家出生位置
```

地形必须先于装饰生成，因为树木和花朵都依赖 `get_surface_height()`。

## 10. 性能与正式架构

当前 Demo 使用 GridMap，约生成 40–90 万个占用单元（取决于地层厚度），仍适合验证玩法，但后续扩大世界时应迁移为 Chunk：

```text
16×16×16 Chunk
PackedByteArray 保存方块ID
按 Chunk 生成可见面网格
Greedy Meshing 合并共面
每个 Chunk 使用一个合并碰撞体
装饰参与同一体素数据，不创建独立节点
```

高度图可用于快速寻找地表和放置装饰，但不能替代完整体素数据，因为玩家会破坏地下方块并形成洞穴。

优化顺序建议：

1. 世界分 Chunk，只生成玩家附近区块。
2. 邻面剔除，删除被包围方块的不可见面。
3. Greedy Meshing 合并草、泥土、石头大平面。
4. 装饰分批生成，避免首帧阻塞。
5. 保存世界种子和玩家修改差异，而不是保存整个初始世界。

## 11. 验证要点

自动化测试应覆盖：

- 高度图固定为 250×250。
- 所有高度位于 `-3..+5`，且存在多个不同高度。
- 任意采样列的地表是草、下方 1–3 层泥土、再下方为石头。
- 出生点 8 格范围完全平坦。
- 树木和花朵避开出生安全区。
- 每棵树有 3–5 格原木和足够大的树冠。
- 花朵位于草地上方、使用十字平面且无碰撞。
- 相同种子生成完全一致的高度图、树木和花朵位置。
- 原有移动、碰撞、瞄准、破坏、放置、背包和合成功能继续通过回归测试。
