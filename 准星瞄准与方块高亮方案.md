# 《哼哼的小小世界》准星瞄准与方块高亮方案

## 1. 功能拆分

瞄准系统分成三个互不混淆的层次：

1. **2D 准星**：始终显示在屏幕正中央，只表示视线中心。
2. **3D 物理射线**：从 Camera3D 向前检测 8 米内的方块。
3. **3D 方块高亮**：只有射线命中有效方块时才显示，并跟随命中单元移动。

准星不应该因为没有命中方块而消失；高亮也不应该作为 UI 绘制，否则无法正确贴合三维方块。

## 2. 2D 十字准星

### 节点选择

准星放在 HUD 的 `CanvasLayer` 中：

```text
HUD (CanvasLayer)
└── Root (Control, Full Rect)
    └── Crosshair (Control, Center Anchor)
        ├── ShadowTop (ColorRect)
        ├── ShadowBottom (ColorRect)
        ├── ShadowLeft (ColorRect)
        ├── ShadowRight (ColorRect)
        ├── WhiteTop (ColorRect)
        ├── WhiteBottom (ColorRect)
        ├── WhiteLeft (ColorRect)
        └── WhiteRight (ColorRect)
```

`CanvasLayer` 使 UI 独立于三维相机。无论摄像机如何移动、旋转或改变 FOV，准星都固定在屏幕中央。

根 `Control` 使用全屏锚点，`Crosshair` 使用 `PRESET_CENTER`。当前准星为 24×24 像素，中央留有小间隙：

- 白色主体厚度：2 像素。
- 黑色半透明描边厚度：4 像素。
- 上下左右各一段短线。
- 所有 Control 都设置 `MOUSE_FILTER_IGNORE`，不拦截游戏鼠标输入。

项目使用 `ColorRect` 而不是 Label 的 `+` 字符。这样不依赖字体字形、字号基线和 CJK 回退字体，在不同系统和分辨率下更稳定。

也可以改成一张透明 PNG 的 `TextureRect`，但简单十字无需额外素材。

## 3. 摄像机射线检测

### 当前实现方式

在玩家每个物理帧移动完成后，使用 `PhysicsDirectSpaceState3D.intersect_ray()`：

```gdscript
var origin := camera.global_position
var direction := -camera.global_transform.basis.z.normalized()
var end := origin + direction * 8.0

var query := PhysicsRayQueryParameters3D.create(
    origin,
    end,
    WORLD_COLLISION_LAYER,
    [player]
)
query.collide_with_areas = false
query.collide_with_bodies = true

var result := get_world_3d().direct_space_state.intersect_ray(query)
```

Godot Camera3D 的前方是局部 `-Z`。必须使用 `global_transform.basis.z`，这样身体的水平 yaw 与 Head 的垂直 pitch 都会参与射线方向计算。

射线参数：

- 长度：`8.0m`
- 碰撞掩码：世界层 1
- 排除对象：玩家自身
- 检测物理 Body：是
- 检测 Area：否

使用代码射线而不是 RayCast3D 的原因：

- 起点和终点在一个函数里清晰可见。
- 可以立即刷新，适合破坏/放置后重新检测。
- 测试中可直接调用 `update_target_block()`。

如果以后需要在编辑器中可视化射线，也可以在 Camera3D 下增加 `RayCast3D`：

```text
Head
└── Camera3D
    └── BlockRayCast3D
        target_position = Vector3(0, 0, -8)
        collision_mask = 1
```

两种方式二选一，不要同时维护两套命中结果。

## 4. 从命中点换算方块坐标

射线结果包含：

- `position`：世界空间命中点。
- `normal`：命中表面的世界法线。
- `collider`：被命中的 GridMap。

命中点经常恰好位于两个体素单元边界。因此先沿法线向方块内部移动 0.01 米，再转换坐标：

```gdscript
var inside_point := hit_position - hit_normal * 0.01
var local_point := world.to_local(inside_point)
var target_cell := world.local_to_map(local_point)
```

同时将法线转换为整数方向：

```gdscript
var target_normal := Vector3i(
    roundi(hit_normal.x),
    roundi(hit_normal.y),
    roundi(hit_normal.z)
)
```

这样：

- 左键破坏 `target_cell`。
- 右键放置到 `target_cell + target_normal`。
- 高亮定位到 `world.map_to_local(target_cell)`。

只有 `collider == VoxelWorld` 时才视为有效方块命中，避免以后加入道具、敌人或触发区后误高亮。

## 5. 方块边缘线框高亮

### 当前选择：整个方块的 12 条边

高亮节点位于 VoxelWorld 下：

```text
VoxelWorld (GridMap)
└── BlockHighlight (MeshInstance3D)
```

它使用一个 `ArrayMesh`，拓扑类型为 `Mesh.PRIMITIVE_LINES`。代码创建立方体 8 个角点，再按 12 条边写入 24 个线段顶点。

线框尺寸设为 `1.012m`，比方块略大，避免与方块表面完全重合产生 Z-fighting。材质设置：

- `SHADING_MODE_UNSHADED`：不受日照明暗影响。
- 半透明黄：在草地、泥土和石头上都醒目。
- 不投射阴影。
- `no_depth_test = true`：边缘不会因贴得过近而闪烁。

命中时：

```gdscript
highlight.position = map_to_local(target_cell)
highlight.visible = true
```

未命中时：

```gdscript
highlight.visible = false
```

只复用一个高亮节点，不为每个方块创建高亮对象。

### 为什么选择整个方块边缘

整个方块边缘能明确告诉玩家左键将破坏哪个方块，比只显示命中面更不容易混淆。它还不遮挡方块贴图，只增加少量线段绘制。

## 6. 可选方案：只高亮命中的面

如果想强调右键将在该表面外侧放置方块，可用一个略大于 1×1 的 `QuadMesh`：

```text
VoxelWorld
└── FaceHighlight (MeshInstance3D)
```

根据 `target_normal` 设置位置和旋转：

- `(0, 1, 0)`：方块顶面。
- `(0, -1, 0)`：底面。
- `(±1, 0, 0)`：左右侧面。
- `(0, 0, ±1)`：前后侧面。

面片中心位置：

```gdscript
map_to_local(target_cell) + Vector3(target_normal) * 0.506
```

材质使用 15%～25% 透明度的黄色或白色，并关闭光照。也可以同时使用“边缘线框 + 命中面淡色覆盖”，但视觉不要太亮。

## 7. 每帧状态流程

```text
玩家 move_and_slide()
        ↓
取得 Camera3D 世界位置与 -Z 方向
        ↓
向前发射最长 8 米物理射线
        ↓
是否命中 VoxelWorld？
  ├─ 否 → has_target=false → 隐藏 BlockHighlight
  └─ 是 → 计算 target_cell / target_normal
                         ↓
              移动并显示 BlockHighlight
```

准星在上述流程之外，始终由 HUD 显示。

## 8. 职责划分

### `scripts/hud.gd`

- 创建 CanvasLayer 下的 2D 准星。
- 保证准星固定在屏幕中心。
- 不参与物理射线和方块坐标计算。

### `scripts/player.gd`

- 从 Camera3D 发射射线。
- 保存 `has_target`、`target_cell`、`target_normal`。
- 决定左键破坏和右键放置目标。
- 通知世界显示或隐藏高亮。

### `scripts/voxel_world.gd`

- 创建和复用线框高亮 MeshInstance3D。
- 将逻辑单元坐标换算成高亮局部位置。
- 不读取鼠标，也不决定玩家瞄准方向。

## 9. 性能与正确性注意事项

- 每个物理帧只发射一条 8 米射线，开销很低。
- 射线只检测世界层，避免无关碰撞对象。
- 全世界只存在一个高亮 MeshInstance3D。
- 线框略微放大，避免 Z-fighting。
- 破坏或放置后立即重新射线检测，避免高亮停留在已经不存在的方块上。
- 当窗口大小变化时，中心锚点会自动保持准星居中。
- 正式 Chunk 网格替换 GridMap 后，只要射线 collider 能映射回体素坐标，HUD 和高亮状态机无需重写。
