# 《哼哼的小小世界》破坏与放置方块方案

## 1. 交互前提：必须存在有效高亮目标

玩家脚本维护三项瞄准状态：

```gdscript
var has_target: bool
var target_cell: Vector3i
var target_normal: Vector3i
```

- `has_target`：8 米摄像机射线是否命中 VoxelWorld。
- `target_cell`：当前被黄色线框高亮的方块坐标。
- `target_normal`：射线击中的表面朝外法线。

只有 `has_target == true` 时，左键和右键才允许修改世界。这样操作目标与屏幕反馈严格一致：玩家看到哪个方块高亮，就只会操作那个方块。

## 2. 输入映射与处理

当前交互在玩家的 `_unhandled_input(event)` 中处理：

```text
鼠标左键         → try_break_target_block()
鼠标右键         → try_place_target_block()
滚轮向上         → cycle_slot(-1)
滚轮向下         → cycle_slot(+1)
数字键 1～5      → select_slot(0～4)
```

鼠标未被游戏捕获时，第一次点击只重新捕获鼠标，不执行破坏或放置，避免玩家点击窗口时误操作。

滚轮采用循环切换：

```text
草 → 泥土 → 石头 → 木板 → 砖块 → 草
```

反向滚轮按相反顺序循环。

## 3. 五种可放置方块

方块使用整数 ID：

```gdscript
const AIR := -1
const GRASS := 0
const DIRT := 1
const STONE := 2
const PLANKS := 3
const BRICKS := 4
```

玩家快捷栏：

```gdscript
const BLOCK_SLOTS := [GRASS, DIRT, STONE, PLANKS, BRICKS]
```

HUD 根据 `BLOCK_SLOTS` 动态创建五个槽位，而不是写死槽位数量。砖块采用红褐色像素纹理，并由代码加入错列砖缝。

## 4. 左键破坏流程

```gdscript
func try_break_target_block() -> bool:
    if not has_target or not world.has_block(target_cell):
        return false
    var removed := world.remove_block(target_cell)
    update_target_block()
    return removed
```

完整流程：

1. 检查当前是否存在高亮目标。
2. 再确认目标坐标仍然存在方块，防止同一帧内状态过期。
3. 调用 `world.remove_block(target_cell)`。
4. 世界把该单元设置为 `AIR`。
5. 立即重新进行射线检测。
6. 原目标消失后，高亮会隐藏或移到后方另一个方块。

删除不是释放某个方块 Node，因为世界中没有“一方块一节点”。它是把整数网格坐标的数据从方块 ID 改成 AIR。

## 5. 右键放置位置计算

### 5.1 面法线是相邻格偏移

方块世界是严格正交网格，因此命中面法线经过取整后只会是六种方向之一：

```text
上表面：  ( 0,  1,  0)
下表面：  ( 0, -1,  0)
右表面：  ( 1,  0,  0)
左表面：  (-1,  0,  0)
前表面：  ( 0,  0,  1)
后表面：  ( 0,  0, -1)
```

新方块位置直接计算为：

```gdscript
var place_cell: Vector3i = target_cell + target_normal
```

示例：

```text
高亮方块坐标 = (10, 2, 5)
命中顶面法线 = (0, 1, 0)
新方块坐标   = (10, 3, 5)
```

如果命中右侧面：

```text
高亮方块坐标 = (10, 2, 5)
命中面法线   = (1, 0, 0)
新方块坐标   = (11, 2, 5)
```

这比根据浮点命中位置自行加减 1 米更稳健，不会受到摄像机角度或浮点误差影响。

### 5.2 放置检查顺序

```gdscript
func try_place_target_block() -> bool:
    if not has_target:
        return false

    var place_cell := target_cell + target_normal

    if world.has_block(place_cell):
        return false
    if cell_overlaps_player(place_cell):
        return false

    var placed := world.place_block(place_cell, selected_block)
    update_target_block()
    return placed
```

顺序为：

1. 必须有高亮目标。
2. 计算相邻单元。
3. 相邻单元必须为空气。
4. 该 1 米立方体不能与玩家碰撞体积重叠。
5. 写入玩家当前选择的方块 ID。
6. 立即重新检测高亮目标。

## 6. 防止方块放进玩家身体

不再使用简单的坐标距离近似，而使用 Godot 物理形状查询。

待放置方块创建临时 `BoxShape3D`：

```gdscript
var block_shape := BoxShape3D.new()
block_shape.size = Vector3.ONE * 0.998
```

查询参数：

```gdscript
var query := PhysicsShapeQueryParameters3D.new()
query.shape = block_shape
query.transform = Transform3D(
    Basis.IDENTITY,
    world.to_global(world.map_to_local(place_cell))
)
query.collision_mask = PLAYER_COLLISION_LAYER
query.collide_with_bodies = true
query.collide_with_areas = false
```

然后调用：

```gdscript
var hits := get_world_3d().direct_space_state.intersect_shape(query, 1)
```

如果结果包含当前 Player，放置失败。

查询盒缩小到 `0.998m`，目的是允许新方块表面与玩家脚底或胶囊边界刚好接触；只有体积真正穿入玩家时才拒绝。否则玩家站在地面上时，地面相邻单元与脚底的数学接触也可能被误判为重叠。

## 7. 世界数据更新

VoxelWorld 提供唯一的写入入口：

```gdscript
func set_block(cell: Vector3i, block_type: int) -> bool
```

并发出：

```gdscript
signal block_changed(
    cell: Vector3i,
    previous_type: int,
    new_type: int
)
```

破坏：

```gdscript
set_block(target_cell, AIR)
```

放置：

```gdscript
set_block(place_cell, selected_block)
```

当前 GridMap 实现中，`set_block()` 最终调用：

```gdscript
set_cell_item(cell, block_type)
```

Godot 随后更新对应单元的网格和碰撞。

`block_changed` 信号为以后功能预留统一入口：

- 存档系统记录被修改坐标。
- 音效系统播放破坏或放置声音。
- 粒子系统生成方块碎屑。
- 任务系统统计采集数量。
- 多人系统广播方块变化。
- Chunk 实现标记脏区块并重建网格。

## 8. 正式 Chunk 数据结构如何更新

未来将 GridMap 替换成 Chunk 后，玩家接口不变：

```gdscript
world.remove_block(cell)
world.place_block(cell, selected_block)
```

`set_block()` 内部改为：

1. 将世界单元坐标转换为 Chunk 坐标。
2. 取得 Chunk 内部局部坐标。
3. 计算 PackedByteArray 一维索引。
4. 更新方块 ID。
5. 标记本 Chunk 网格和碰撞为 dirty。
6. 如果方块位于 Chunk 边界，同时标记相邻 Chunk dirty。
7. 在当前帧末或后台任务中重建受影响 Chunk。
8. 将该 Chunk 加入存档修改集合。

伪代码：

```gdscript
func set_block(cell: Vector3i, type: int) -> bool:
    var chunk_coord := world_to_chunk(cell)
    var local_cell := world_to_local_cell(cell)
    var chunk := get_or_create_chunk(chunk_coord)
    var previous := chunk.get_block(local_cell)

    if previous == type:
        return false

    chunk.set_block(local_cell, type)
    mark_chunk_dirty(chunk_coord)
    mark_boundary_neighbors_dirty(chunk_coord, local_cell)
    block_changed.emit(cell, previous, type)
    return true
```

玩家脚本完全不需要知道世界内部使用 GridMap、PackedByteArray 还是合并 ArrayMesh。

## 9. 状态流程

```text
Camera3D 射线命中
       ↓
保存 target_cell + target_normal
       ↓
显示高亮线框
       ↓
┌───────────────┬────────────────────┐
│ 左键          │ 右键               │
│ target→AIR    │ target+normal      │
│               │ 检查为空气         │
│               │ 检查不与玩家重叠   │
│               │ 写入 selected_block│
└───────────────┴────────────────────┘
       ↓
更新世界网格与碰撞
       ↓
立即重新执行射线检测
```

如果射线没有命中：`has_target=false`，左右键函数立即返回 false，世界数据保持不变。

## 10. 职责边界

### 玩家脚本

- 保存瞄准目标和面法线。
- 接收左右键、滚轮和数字键。
- 计算 `target_cell + target_normal`。
- 检查玩家体积重叠。
- 调用世界公开接口。

### 世界脚本

- 验证方块 ID。
- 维护坐标到方块类型的映射。
- 拒绝向非空气单元放置。
- 更新网格与碰撞。
- 发出方块变化信号。

### HUD 脚本

- 显示 5 个快捷栏槽位。
- 显示当前选中方块。
- 不直接修改世界。

这种分工确保输入、规则、数据和显示不会互相耦合。
