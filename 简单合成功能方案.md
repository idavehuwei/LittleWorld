# 《哼哼的小小世界》简单合成功能方案

## 1. 功能范围

在现有 36 格背包界面上增加：

- 2×2 合成输入网格，共 4 格。
- 一个只读输出格。
- 输入变化后自动匹配配方并显示预览。
- 点击输出格后，将结果放入背包并消耗一轮材料。

初始配方：

```text
任意单格放入 1 个木头 → 4 个木板
2×2 四格各放入 1 个木板 → 1 个工作台
```

本阶段新增物品 ID：

```gdscript
ItemCatalog.LOG = 100
ItemCatalog.CRAFTING_TABLE = 101
```

木板继续复用可放置方块 `VoxelWorld.PLANKS = 3`。木头与工作台当前作为背包/合成物品，暂不接入世界放置；后续可为其注册对应方块 MeshLibrary 项。

## 2. 系统分层

```text
ItemCatalog
├── 物品名称
├── 图标路径
└── 堆叠上限

CraftingRecipe
├── id
├── width / height
├── pattern
├── result_item
└── result_amount

RecipeBook
├── recipes[]
└── find_match(grid)

CraftingGrid
├── item_ids[4]
├── amounts[4]
├── matched_recipe
├── move_one_from_inventory()
├── return_one_to_inventory()
└── craft_once()

GameHUD
├── 36格背包
├── 2×2输入格
└── 输出格
```

职责边界：

- `ItemCatalog` 只定义物品元数据。
- `CraftingRecipe` 是不可变配方描述。
- `RecipeBook` 负责匹配，不修改背包和材料。
- `CraftingGrid` 持有运行时材料并执行合成事务。
- `GameHUD` 只处理点击和显示，通过公开方法修改数据。

## 3. 配方数据组织

有形配方使用“宽、高、行主序图案、输出”表示：

```gdscript
CraftingRecipe.new(
    &"planks_to_crafting_table",
    2,
    2,
    PackedInt32Array([
        VoxelWorld.PLANKS, VoxelWorld.PLANKS,
        VoxelWorld.PLANKS, VoxelWorld.PLANKS,
    ]),
    ItemCatalog.CRAFTING_TABLE,
    1
)
```

木头转木板定义成 1×1：

```gdscript
CraftingRecipe.new(
    &"log_to_planks",
    1,
    1,
    PackedInt32Array([ItemCatalog.LOG]),
    VoxelWorld.PLANKS,
    4
)
```

这种结构比为每个配方写独立 `if` 更适合扩展：新增配方只需加入数据，不必修改匹配算法。

正式项目中可把 `CraftingRecipe` 改成自定义 `Resource`，每张配方保存为 `.tres`，由 `RecipeBook` 扫描目录加载。这样策划可以在编辑器中维护配方。

## 4. 2×2 网格表示

合成输入采用两个长度固定为 4 的 `PackedInt32Array`：

```gdscript
item_ids = [左上, 右上, 左下, 右下]
amounts  = [数量, 数量, 数量, 数量]
```

空格使用 `PlayerInventory.EMPTY_ITEM = -1`，数量必须为 0。

配方匹配只看“哪些格子是什么物品”，单格中的额外数量不影响预览；点击输出时每个被配方占用的格子消耗 1 个。这样一格放入 8 个木头后，可以连续点击输出 8 次，每次获得 4 个木板。

## 5. 配方匹配算法

`RecipeBook.find_match()` 遍历配方，并尝试将配方矩形平移到 2×2 网格中的每个合法偏移：

```gdscript
for offset_y in range(3 - recipe.height):
    for offset_x in range(3 - recipe.width):
        if matches_at(grid, recipe, offset_x, offset_y):
            return recipe
```

匹配规则：

1. 配方区域内的物品 ID 必须完全一致。
2. 配方区域外的网格格子必须为空。
3. 1×1 木头配方可放在四个格子的任意一个。
4. 2×2 工作台配方必须填满全部四格。
5. 多余材料会导致不匹配，输出格清空。

当前两张配方不需要镜像。未来加入斧头、楼梯等方向性配方时，可给配方增加 `allow_mirror`，匹配时额外尝试水平翻转图案。

无序配方（例如任意位置的煤+木棍）不应套用有形矩阵匹配；可以增加 `ShapelessRecipe`，先统计各物品数量，再比较多重集合。

## 6. 输出预览

每次输入格发生变化后，`CraftingGrid._refresh_match()`：

```text
将4格转换为物品图案
    ↓
RecipeBook.find_match()
    ↓
保存 matched_recipe
    ↓
发出 grid_changed / output_changed
```

HUD 收到信号后：

- 有匹配：输出格显示结果图标和数量。
- 无匹配：清空输出格并禁用按钮。

输出格只是配方结果的实时预览，不直接存储独立物品堆栈，因此不会出现“材料已变化但旧输出仍留在格子里”的过期状态。

## 7. UI 点击流程

当前采用适合最小 Demo 的“点击移动”，同时保留以后改为拖拽的接口边界：

### 背包 → 合成格

1. 玩家在完整背包中点击一个物品槽，HUD 记录 `held_slot_index`。
2. 玩家点击某个合成输入格。
3. `CraftingGrid.move_one_from_inventory(source, grid_slot)` 从背包扣 1 个并放入合成格。
4. 自动重新匹配配方并刷新输出。

### 合成格 → 背包

当没有选中背包源槽位时，点击合成格：

1. 检查背包能否容纳 1 个对应材料。
2. 成功入包后，合成格数量减 1。
3. 数量归零时清空该格。
4. 自动重新匹配。

### 点击输出格

1. 记录当前匹配结果物品和数量。
2. 调用 `CraftingGrid.craft_once()`。
3. 成功后显示“合成成功”提示。
4. 材料数量和输出预览通过信号自动刷新。

Godot 原生拖拽可在后续通过 `Control._get_drag_data()`、`_can_drop_data()` 和 `_drop_data()` 实现；数据操作仍调用相同的 `CraftingGrid` 方法，不需要改配方逻辑。

## 8. 原子合成事务

点击输出不能先扣材料再尝试放入结果，否则背包满时会丢失材料。`craft_once()` 顺序为：

```text
确认存在 matched_recipe
    ↓
Inventory.can_add(result, result_amount)
    ↓ 否
返回失败，不扣材料
    ↓ 是
再次验证材料仍满足配方
    ↓
消耗每个配方格1个材料
    ↓
Inventory.add_item(result, result_amount)
    ↓
刷新配方与输出
```

输出必须能完整放入背包才允许合成。例如木头产生 4 个木板时，只剩 3 个木板容量也会拒绝，避免部分成品和材料结算不一致。

## 9. 背包关闭时的材料处理

当前合成网格是玩家背包界面的持久临时容器：关闭 E 界面时材料仍保留，重新打开后可继续合成。这种方式不会因背包刚好已满而无法关闭界面。

若游戏退出或存档，应一起保存：

```json
{
  "crafting_item_ids": [100, -1, -1, -1],
  "crafting_amounts": [3, 0, 0, 0]
}
```

另一种策略是在关闭界面时调用 `return_all_to_inventory()`；但如果背包没有空间，就必须阻止关闭或生成地面掉落物，流程更复杂。

## 10. 后续扩展

- 右键一次放入一个，左键放入整组。
- Shift+点击自动把物品送入可匹配配方。
- 无序配方和镜像配方。
- 长按输出或 Shift+点击批量合成。
- 工作台方块打开 3×3 合成网格。
- 合成音效、粒子反馈和配方解锁提示。
- 配方书 UI，显示已知配方并一键填充材料。
