# 《哼哼的小小世界》简单合成功能方案

## 1. 功能范围

现有 36 格背包界面包含：

- 2×2 合成材料网格，共 4 格。
- 一个只读成品预览格。
- “预览配方”和“合成”两个独立按钮。
- 可同时显示全部当前可制作配方的候选列表。
- 玩家点击候选成品后，再点击“合成”，只制作被选中的配方。

当前配方：

```text
1 个木头 → 4 个木板
4 个木板 → 1 个工作台
4 个木板 → 2 个砖块（演示同一组材料对应多个成品）
```

本阶段物品 ID：

```gdscript
ItemCatalog.LOG = 100
ItemCatalog.CRAFTING_TABLE = 101
VoxelWorld.PLANKS = 3
VoxelWorld.BRICKS = 4
```

木头与工作台当前作为背包/合成物品，暂未接入世界放置；木板和砖块复用已有可放置方块类型。

## 2. 交互原则

合成不自动执行，也不自动选择结果：

```text
放入材料
  ↓
点击“预览配方”
  ↓
显示全部材料数量足够的候选
  ↓
点击一个候选成品
  ↓
输出格显示所选结果，“合成”按钮启用
  ↓
点击“合成”
  ↓
只扣该配方需要的材料，只产出该配方结果
```

特殊状态：

- 2×2 网格为空时点击预览，提示“请放入2×2材料”。
- 有材料但不满足任何配方时，提示“当前材料不足以制作任何物品”。
- 材料发生任何变化后，旧候选和旧选择立即失效，必须重新预览。
- 背包无法完整容纳结果时拒绝合成，材料不扣除。

## 3. 系统分层

```text
ItemCatalog
├── 物品名称
├── 图标路径
└── 堆叠上限

CraftingRecipe
├── id
├── width / height / pattern（配方录入信息）
├── required_items（运行时数量需求）
├── result_item
└── result_amount

RecipeBook
├── recipes[]
├── find_matches(available_items)
└── can_craft(recipe, available_items)

CraftingGrid
├── item_ids[4] / amounts[4]
├── preview_recipes[]
├── previewed_recipe
├── preview_all_recipes()
├── select_preview(index)
└── craft_once()

GameHUD
├── 36 格背包
├── 2×2 材料格
├── 候选配方按钮列表
├── 只读成品预览格
├── “预览配方”按钮
└── “合成”按钮
```

职责边界：

- `ItemCatalog` 定义物品元数据。
- `CraftingRecipe` 描述一张配方及其材料数量需求。
- `RecipeBook` 只负责查找所有可制作配方，不修改材料或背包。
- `CraftingGrid` 保存运行时材料、候选、选择，并执行原子合成事务。
- `GameHUD` 只负责点击、提示和显示，通过公开方法操作数据模型。

## 4. 配方数据组织

配方仍使用“宽、高、行主序图案、输出”录入，构造时自动把非空图案转换为 `required_items`：

```gdscript
CraftingRecipe.new(
    &"planks_to_crafting_table",
    2,
    2,
    PackedInt32Array([
        VoxelWorld.PLANKS, VoxelWorld.PLANKS,
        VoxelWorld.PLANKS, VoxelWorld.PLANKS,
    ]),
    ItemCatalog.CRAFTING_TABLE,
    1
)
```

预处理结果：

```gdscript
required_items = {
    VoxelWorld.PLANKS: 4
}
```

目前匹配只使用 `required_items`，因此不要求材料位于特定格子，也不要求输入图案与配方完全相等。`width`、`height` 和 `pattern` 保留为配方展示信息，也为以后重新支持“有形配方”预留数据。

正式项目可把 `CraftingRecipe` 改成自定义 `Resource`，每张配方保存为 `.tres`，由 `RecipeBook` 扫描目录加载。

## 5. 2×2 材料网格

输入使用两个长度固定为 4 的 `PackedInt32Array`：

```gdscript
item_ids = [左上, 右上, 左下, 右下]
amounts  = [数量, 数量, 数量, 数量]
```

空格使用 `PlayerInventory.EMPTY_ITEM = -1`，数量为 0。每格可堆叠同类物品，最多遵循 `ItemCatalog.stack_limit()`。

预览时将四格按物品 ID 汇总：

```gdscript
available[item_id] += amounts[index]
```

例如：

```text
左上：木板 ×3
右上：石头 ×5
左下：木板 ×5
右下：空
```

汇总为：

```gdscript
{
    VoxelWorld.PLANKS: 8,
    VoxelWorld.STONE: 5
}
```

系统会判断工作台和砖块两个配方都可制作；多出的 4 个木板和 5 个石头不会阻止匹配。

## 6. 数量型多候选匹配

`RecipeBook.find_matches()` 遍历全部配方，并返回所有需求都满足的配方：

```gdscript
func can_craft(recipe, available_items) -> bool:
    for item_id in recipe.required_items:
        if available_items.get(item_id, 0) < recipe.required_items[item_id]:
            return false
    return true
```

匹配规则：

1. 每种必需材料的总数量必须不少于配方需求。
2. 材料可以分布在 2×2 的任意格子。
3. 同一格可以放入多个材料单位。
4. 多余的必需材料允许保留。
5. 与配方无关的材料也允许存在，不影响候选生成。
6. 同一组材料可同时匹配多个不同结果，全部列入候选。

这种语义相当于“无序数量配方”。以后若增加工具、楼梯等必须区分排列方式的有形配方，可为配方增加 `match_mode`，分别走数量匹配和矩阵匹配。

## 7. 手动预览与选择

`CraftingGrid.preview_all_recipes()`：

```text
汇总四格可用数量
    ↓
RecipeBook.find_matches()
    ↓
保存 preview_recipes[]
    ↓
清空 previewed_recipe
    ↓
发出 preview_changed(candidates)
    ↓
输出格保持为空，等待玩家选择
```

HUD 根据 `preview_changed` 动态创建候选按钮。按钮显示成品名称和数量，Tooltip 显示所需材料。

玩家点击候选时调用：

```gdscript
CraftingGrid.select_preview(index)
```

成功后：

- `previewed_recipe` 指向该候选。
- 输出格显示该候选成品。
- “合成”按钮启用。
- 其他候选仍显示，但不会被同时制作。

## 8. UI 点击流程

### 背包 → 合成格

1. 点击一个背包槽，HUD 记录 `held_slot_index`。
2. 点击目标合成格。
3. `move_one_from_inventory(source, grid_slot)` 移入 1 个同类材料。
4. 材料变化使旧候选和选择失效。
5. 玩家可继续放材料，完成后手动点击“预览配方”。

### 合成格 → 背包

未选中背包源槽时点击合成格：

1. 检查背包能否容纳 1 个对应材料。
2. 成功入包后合成格数量减 1。
3. 数量归零时清空该格。
4. 旧候选和选择失效。

### 预览与合成

1. 点击“预览配方”生成所有候选。
2. 点击一个候选，确定本次目标配方。
3. 点击“合成”。
4. 系统重新验证该配方仍满足。
5. 成功后只扣除所选配方需求并将结果放入背包。

## 9. 原子合成事务

`craft_once()` 执行顺序：

```text
确认已选择 previewed_recipe
    ↓
按当前材料重新查找候选，并按 id 找回所选配方
    ↓ 不再满足
清空过期预览并拒绝
    ↓ 仍满足
Inventory.can_add(result, result_amount)
    ↓ 容量不足
拒绝，不扣材料，保留选择以便清理背包后重试
    ↓ 容量足够
按 required_items 精确扣除所选配方数量
    ↓
Inventory.add_item(result, result_amount)
    ↓
清空候选和选择，提示重新预览
```

扣除材料时按格子索引依次消费相同物品，直到满足该物品需求。无关材料和多余数量保持不变。

例如 8 个木板同时匹配工作台与砖块：

- 选择工作台：扣 4 木板，获得 1 工作台。
- 选择砖块：扣 4 木板，获得 2 砖块。
- 绝不会一次点击同时产出两个候选。

## 10. 背包关闭与后续扩展

合成网格是持久临时容器：关闭 E 界面时材料仍保留，重新打开后可继续操作；候选状态可因材料变化而失效，但材料不会自动退回。

后续可扩展：

- 候选按钮增加物品图标、配方材料明细和可制作次数。
- Shift+点击批量合成所选配方。
- 配方优先级、解锁条件和分类筛选。
- `match_mode = SHAPELESS / SHAPED` 同时支持数量配方与有形矩阵配方。
- 工作台方块打开 3×3 合成网格。
- 原生拖拽：`Control._get_drag_data()`、`_can_drop_data()`、`_drop_data()`。
- 合成音效、粒子反馈和配方书。
